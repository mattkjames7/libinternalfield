#ifndef __COEFFS_H__
#define __COEFFS_H__
#include <vector>
#include <string>
#include <map>
#endif

/* list of model names */
extern std::vector<std::string> modelNames;

/* pointers to the memory where coefficients are stored */
